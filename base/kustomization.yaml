apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - ingress.yaml
  - serviceaccount.yaml

commonLabels:
  app.kubernetes.io/name: growth-engine
  app.kubernetes.io/component: api
  app.kubernetes.io/part-of: growth-engine
