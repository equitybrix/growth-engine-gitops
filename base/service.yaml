apiVersion: v1
kind: Service
metadata:
  name: growth-engine-api
  namespace: growth-engine
  labels:
    app: growth-engine
    component: api
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: growth-engine
    component: api
