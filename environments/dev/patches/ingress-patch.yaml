apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: growth-engine-api
  namespace: growth-engine-dev
spec:
  tls:
    - hosts:
        - dev.growth-engine.equitybrix.net
      secretName: growth-engine-dev-tls
  rules:
    - host: dev.growth-engine.equitybrix.net
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: growth-engine-api
                port:
                  number: 8000
