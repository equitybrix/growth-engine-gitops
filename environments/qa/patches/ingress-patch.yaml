apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: growth-engine-api
  namespace: growth-engine-qa
spec:
  tls:
    - hosts:
        - qa.growth-engine.equitybrix.net
      secretName: growth-engine-qa-tls
  rules:
    - host: qa.growth-engine.equitybrix.net
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: growth-engine-api
                port:
                  number: 8000
